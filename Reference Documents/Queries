Supported Query Patterns for Dependencies:

==========================================
1. ARROW SYNTAX (EXPLICIT INSTRUCTIONS) - NEW!
==========================================

Format: Action -> Instruction1 -> Instruction2

Example 1: Extract and Use
Create a new pet -> Retrieve the pet_id from the response
Update pet information -> Use the pet_id from previous response
Delete a pet -> Use the pet_id from previous response

Example 2: Multiple Fields
Create a user -> Extract the user_id and email from response
Create order -> Use the user_id from previous response
Update order -> Use the same user_id

Example 3: Specific Field Names
Create a new pet -> Save the id from response
Update pet -> Pass the id from previous response to this request
Delete pet -> Send the id from previous response

Supported Keywords:
- Extract: retrieve, extract, save, get
- Use: use, pass, send, with


==========================================
2. EXPLICIT STEP REFERENCE
==========================================

Create a new pet in the pet store
Update pet from step 1
Delete pet from step 1


==========================================
3. "PREVIOUS" KEYWORD
==========================================

Create a new pet in the pet store
Update the previous pet
Delete the previous pet


==========================================
4. "CREATED/UPDATED/SAME" KEYWORDS
==========================================

Create a new pet in the pet store
Update the created pet
Delete the same pet


==========================================
5. IMPLICIT (AUTOMATIC)
==========================================

Create a new pet in the pet store
Update the pet information
Delete a pet

(Automatically detects {petId} path parameter and uses extracted ID)


==========================================
EXAMPLES BY USE CASE
==========================================

Use Case 1: Simple CRUD with Arrow Syntax
-----------------------------------------

Create a new pet -> Retrieve the pet_id from the response
Update the pet -> Use the pet_id from previous response
Delete the pet -> Use the pet_id from previous response


Use Case 2: User and Order Workflow
-----------------------------------

Create a new user -> Extract the user_id from response
Create an order -> Use the user_id from previous response
Get order details -> Use the order_id from step 2
Cancel the order -> Use the order_id from previous response


Use Case 3: Implicit Dependencies
---------------------------------

Create a new pet
Update the pet information
Delete a pet

(Works automatically without explicit instructions)


==========================================
TIPS
==========================================

1. Use arrow syntax (->) for explicit control over field extraction
2. Field names are case-insensitive and underscore-flexible (pet_id = petId)
3. Multiple queries separated by semicolon (;)
4. Extraction happens automatically for POST/PUT if subsequent steps need IDs
5. You can mix patterns (some with arrows, some without)


Create a new pet in pest store -> Retrieve the pet_id from the response; Update pet information -> Use the pet_id from previous response; Delete a pet -> Use the pet_id from previous response
test25

Create a new pet in pest store -> Use a unique pet name by generating a random string while creating new pet -> Retrieve the pet_id from the response; Update pet information -> Use the pet_id from previous response while updating, and also use another unique random pet name generated while updating the pet name; Delete a pet -> Use the pet_id from previous response
test26

Find all pets by status available -> Validate all the responded pet records with availble status only



Obtain the openid access token -> use client_id as synaptix-portal, username as admin.synaptix, grant_type as password and password as admin123, and also use realm as synaptix-technologies -> Retrive the access token as bearerToken; Get the list of Get group hierarchy -> Use the above retrieved bearer token authorization -> Validate the group names existing in the list and expected are - Employees, IT, Finance, HR, Management

Obtain the openid access token -> use client_id as synaptix-portal, username as john.employee, grant_type as password and password as employee123, and also use realm as synaptix-technologies -> Retrive the access token as bearerToken; Get the existing list of Get group hierarchy-> Use the bove retrieved bearer token authorization -> Validate the response has 403 return code, and the response has error message with HTTP 403 Forbidden




Directus:
=========

FoldersCrud
-----------

Authenticate Login to retrieve temporary access_token -> Use the "email": "admin@example.com" and "password": "admin123", but do not include mode and otp. Also Retrieve the access_token from the response; Get the list of folders -> Use the above retrieved access_token as bearer token, do not include any query parameters to the request -> Validate the response has the name "aug28", if it is not found in the response then fail it with appropriate message.

Authenticate Login to retrieve temporary access_token -> Use the "email": "admin@example.com" and "password": "admin123", but do not include mode and otp. Also Retrieve the access_token from the response; Create a new folder -> Use the above retrieved access_token as bearer token, Use a random alphabetical string to provide as an input to the folder name, but don't include parent option in the payload, Retrieve the ID from the response; Get the list of folder names -> Use the above retrieved access_token as bearer token -> Validate that the response has a record with the above generated and retrieved ID along with the above used folder name; Delete a folder using folder id -> Use the above retrieved access_token as bearer token -> Use  the same above retrieved ID  to delete in this request; Get the list of folder names -> Use the above retrieved access_token as bearer token -> Validate that the response NOT included with the record having above created folder name and retrived ID, if it is existing by chance then throw with an appropriate error

Authenticate Login to retrieve temporary access_token -> Use the "email": "admin@example.com" and "password": "admin123", but do not include mode and otp. Also Retrieve the access_token from the response;
Create a new folder -> Use the above retrieved access_token as bearer token, Use a random alphabetical string to provide as an input to the folder name, but don't include parent option in the payload, Retrieve the ID from the response;
Update a folder -> Use the above retrieved access_token as bearer token, Use a random alphabetical string to provide as an input to the folder name, but don't include parent option in the payload -> Validate that the response has ID which is as same as above retrieved ID, and the folder name has changed to the one which has been input in this step only.
Get the list of folder names -> Use the above retrieved access_token as bearer token -> Validate that the response has a record with the above generated and retrieved ID along with the above used folder name in step 3;
Delete a folder using folder id -> Use the above retrieved access_token as bearer token -> Use the same above retrieved ID to delete in this request;
Get the list of folder names -> Use the above retrieved access_token as bearer token -> Validate that the response NOT included with the record having above created folder name in step 2 , NOT even in Step 3 along with retrived ID, if it is existing by chance then throw with an appropriate error

Authenticate Login to retrieve temporary access_token -> Use the "email": "admin@example.com" and "password": "admin123", but do not include mode and otp. Also Retrieve the access_token from the response;
Create a new folder -> Use the above retrieved access_token as bearer token, Use a random alphabetical string to provide as an input to the folder name, but don't include parent option in the payload, Retrieve the ID from the response;
Create a new folder -> Use the above retrieved access_token as bearer token, Use a random alphabetical string to provide as an input to the folder name, and also include the retrieved folder id from above step 2 as a parent id in the payload, Retrieve the newly generated ID from the response as well;
Update a folder -> Use the above retrieved access_token as bearer token, Use another random alphabetical string to provide as an input to the folder name, the folder id retrieved from step 3 needs to be updated, also include id retrieved from step 2 as parent -> Validate that the response has the same ID which is as same as above retrieved ID from step 3 along with parent as same as retrieved id from step 2, and the folder name has changed to the one which has been input in this step only;
Get the list of folder names -> Use the above retrieved access_token as bearer token -> Validate that the response has a record with the above generated and retrieved ID from step 3 along with the above used folder name in step 4, also it has included with parent id from step 2;
Delete a folder using folder id -> Use the above retrieved access_token as bearer token -> Use the same above retrieved ID from step 2 to delete in this request;
Delete a folder using folder id -> Use the above retrieved access_token as bearer token -> Use the same above retrieved ID from step 3 to delete in this request;
Get the list of folder names -> Use the above retrieved access_token as bearer token -> Validate that the response does NOT include the record having above retrived ID from step 2 , NOT even the retrieved ID from step 3 , if it is existing by chance then throw with an appropriate error





