


Once after generating a code in a specific test method, if at all I want to delete a method, the one which is created, so from the UI itself, I do not have an option to delete a particular method. Hence, and the utility needs to be prepared to remove the method in the generator ALT util.

Similarly, if I want to delete a particular test file from the folder completely, so I should have an option to delete a test file from the UI itself.

Also, I should get an option to delete a whole component. So, when I delete the component, if there are any files exisring in the folder then those files also should get delete successfully. In that way I want to implement utility function in the generator alt util only.

Currently, we are having the same class name generated in all the test files when we generate the code. Therefore, after completing the code generation of a particular test file, if at all I want to rename the class name, so with the help of all the given queries or the test cases, prompt messages, I should get an utility to generate a one-word class name generated dynamically just by one click in the UI application. So accordingly, I have to implement generator altutil and the front-end option.

In the code generator itself, if at all I want to load all the existing REST test files, I should be able to load into the code generator tab. Accordingly I have to implement a loader util freshly and also the relevant UI option.

Update the feature of clearing the past reports before generaitng new reports.

I have to implement a executor- method level, class level and the component level in the code generator tab itself. Accordingly I should even get the log tabs. So for that I have to implement a minor updates to the UI.

Auto healing - After the execution of a particular test method or a class, if there are any modification needs to be done in the code because of the code failure, the auto healing should be implemented successfully. For that, I guess I have to generate a third level of utility to accept the logs as an input and to produce the output of an updated code and followed by to do the re-execution with maximum of three iterations or the number of iterations set by the user.

Exploration of director's application and the relevant APIs to implement in bulk execution successfully.

I have to implement a executor tab for the bulk execution purpose successfully





